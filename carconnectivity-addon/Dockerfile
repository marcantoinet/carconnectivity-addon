ARG BUILD_FROM
FROM ${BUILD_FROM}
ARG BUILD_ARCH
ARG BUILD_DESCRIPTION
ARG BUILD_NAME
ARG BUILD_VERSION
ARG AUDI_VERSION
ARG SEAT_VERSION
ARG SKODA_VERSION
ARG VW_VERSION
ARG VWNA_VERSION
ARG TRONITY_VERSION
ARG VOLVO_VERSION
ARG ABRP_VERSION
ARG MQTT_VERSION
ARG MQTTHA_VERSION
ARG WEBUI_VERSION
ARG CC_VERSION
COPY rootfs /
WORKDIR /tmp/
RUN echo "ADDON_VERSION=v${BUILD_VERSION}" >> versions.txt && \
    apk add --no-cache --update musl curl nginx py3-pip build-base libffi-dev && \
    python3 -m venv /opt/venv && \
    /opt/venv/bin/pip install --upgrade pip && \
    /opt/venv/bin/pip install --no-cache \
        carconnectivity==${CC_VERSION#v} \
        carconnectivity-connector-seatcupra==${SEAT_VERSION#v} \
        carconnectivity-connector-skoda==${SKODA_VERSION#v} \
        carconnectivity-connector-volkswagen==${VW_VERSION#v} \
        carconnectivity-connector-audi==${AUDI_VERSION#v} \
        carconnectivity-connector-volkswagen_na==${VWNA_VERSION#v} \
        carconnectivity-connector-tronity==${TRONITY_VERSION#v} \
        carconnectivity-plugin-abrp==${ABRP_VERSION#v} \
        carconnectivity-plugin-mqtt==${MQTT_VERSION#v} \
        carconnectivity-plugin-webui==${WEBUI_VERSION#v} \
        carconnectivity-plugin-mqtt_homeassistant==${MQTTHA_VERSION#v} && \
    apk del build-base libffi-dev && \
    echo "CARCONNECTIVITY_VERSION=${CC_VERSION}" >> versions.txt && \
    echo "AUDI_VERSION=${AUDI_VERSION}" >> versions.txt && \
    echo "SEAT_VERSION=${SEAT_VERSION}" >> versions.txt && \
    echo "SKODA_VERSION=${SKODA_VERSION}" >> versions.txt && \
    echo "VW_VERSION=${VW_VERSION}" >> versions.txt && \
    echo "VWNA_VERSION=${VWNA_VERSION}" >> versions.txt && \
    echo "TRONITY_VERSION=${TRONITY_VERSION}" >> versions.txt && \
    echo "VOLVO_VERSION=${VOLVO_VERSION}" >> versions.txt && \
    echo "ABRP_VERSION=${ABRP_VERSION}" >> versions.txt && \
    echo "WEBUI_VERSION=${WEBUI_VERSION}" >> versions.txt && \
    echo "MQTT_VERSION=${MQTT_VERSION}" >> versions.txt && \
    echo "MQTTHA_VERSION=${MQTTHA_VERSION}" >> versions.txt && \
    chmod -R a+rw /tmp && \
    mkdir -p /run/nginx && \
    chmod +x /tmp/entrypoint.sh

# ── CA SPIN fix ────────────────────────────────────────────────────────────────
# Replaces self.session.user_id (IDP ssoid) with backend-local sub from
# the access token JWT for SpinService and RRS calls on the ca00 backend.
# See: https://github.com/zackcornelius/CarConnectivity-connector-volkswagen-na
COPY patches/connector.py /opt/venv/lib/python3.14/site-packages/carconnectivity_connectors/volkswagen_na/connector.py
COPY patches/openid_session.py /opt/venv/lib/python3.14/site-packages/carconnectivity_connectors/volkswagen_na/auth/openid_session.py
RUN find /opt/venv/lib -name "*.pyc" -path "*/volkswagen_na/*" -delete
# ──────────────────────────────────────────────────────────────────────────────

LABEL \
    io.hass.name="${BUILD_NAME}" \
    io.hass.description="${BUILD_DESCRIPTION}" \
    io.hass.arch="${BUILD_ARCH}" \
    io.hass.type="addon" \
    io.hass.version="${BUILD_VERSION}" \
    org.opencontainers.image.source="${BUILD_SOURCE}" \
    org.opencontainers.image.description="${BUILD_DESCRIPTION}" \
    org.opencontainers.image.title="${BUILD_NAME}" \
    org.opencontainers.image.created="${BUILD_DATE}" \
    org.opencontainers.image.version="${BUILD_VERSION}"
ENTRYPOINT ["/bin/sh", "-c", "/tmp/entrypoint.sh"]
# HEALTHCHECK --interval=1m CMD \
#     [ -f "/tmp/carconnectivity_healthy" ] && \
#     grep -q "^healthy$" "/tmp/carconnectivity_healthy" && \
#     find "/tmp/carconnectivity_healthy" -mmin -2 | grep -q .
